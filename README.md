# Vehicle / Car TypeScript Exercise

## Structure
- `car.ts` — source: `Vehicle` interface + `Car` class implementation
- `tsconfig.json` — TypeScript compiler config
- `dist/car.js` — compiled JavaScript output (already built)

## Run it yourself
```bash
npm install
npx tsc
node dist/car.js
```

Expected output:
```
Car engine started
```
